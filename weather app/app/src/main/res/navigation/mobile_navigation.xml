<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">

    <!-- The implementation of the Fragment for the Home Screen -->
    <fragment
        android:id="@+id/navigation_home"
        android:name="com.app.weathernews.fragment.HomeFragment"
        android:label="@string/title_home"
        tools:layout="@layout/fragment_home" />

    <!-- This is the implementation of the Fragment for the Bookmark Screen -->
    <fragment
        android:id="@+id/navigation_bookmark"
        android:name="com.app.weathernews.fragment.BookmarkFragment"
        android:label="@string/title_bookmark"
        tools:layout="@layout/fragment_bookmark" />
</navigation>
